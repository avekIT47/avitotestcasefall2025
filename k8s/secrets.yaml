apiVersion: v1
kind: Secret
metadata:
  name: pr-reviewer-secrets
  namespace: pr-reviewer
type: Opaque
stringData:
  # ВАЖНО: В production использовать External Secrets Operator или HashiCorp Vault
  # Не храните реальные пароли в git!
  
  database_url: "postgres://postgres:CHANGE_ME@postgres:5432/pr_reviewer?sslmode=require"
  redis_password: ""
  jwt_secret: "CHANGE_ME_TO_RANDOM_SECRET_KEY"
  
  # Пример использования External Secrets Operator:
  # Этот файл будет генерироваться автоматически из Vault/AWS Secrets Manager
---
# Для External Secrets Operator (закомментировано)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: pr-reviewer-secrets
#   namespace: pr-reviewer
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: pr-reviewer-secrets
#     creationPolicy: Owner
#   data:
#   - secretKey: database_url
#     remoteRef:
#       key: pr-reviewer/database
#       property: url
#   - secretKey: redis_password
#     remoteRef:
#       key: pr-reviewer/redis
#       property: password
#   - secretKey: jwt_secret
#     remoteRef:
#       key: pr-reviewer/jwt
#       property: secret

