# 🔍 Конфигурация линтера golangci-lint

> Подробное описание конфигурации статического анализа кода для проекта PR Reviewer Service

---

## 📋 Содержание

- [Общая информация](#общая-информация)
- [Структура конфигурации](#структура-конфигурации)
- [Включенные линтеры](#включенные-линтеры)
- [Настройки линтеров](#настройки-линтеров)
- [Правила исключений](#правила-исключений)
- [Использование](#использование)
- [CI/CD интеграция](#cicd-интеграция)
- [Рекомендации по работе](#рекомендации-по-работе)

---

## 📖 Общая информация

### О golangci-lint

**golangci-lint** - это мощный агрегатор множества линтеров для Go, который:
- Объединяет более 50+ линтеров в одном инструменте
- Обеспечивает быстрый параллельный запуск проверок
- Поддерживает кэширование результатов
- Легко интегрируется в CI/CD pipeline

### Файл конфигурации

Конфигурация находится в файле `.golangci.yml` в корне проекта.

### Версия

Рекомендуемая версия: **golangci-lint 1.55+**

```bash
# Установка
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

# Проверка версии
golangci-lint --version
```

---

## 🏗️ Структура конфигурации

Файл `.golangci.yml` состоит из трех основных секций:

```yaml
run:                    # Общие настройки запуска
linters:                # Список включенных/отключенных линтеров
linters-settings:       # Настройки конкретных линтеров
issues:                 # Правила для обработки найденных проблем
```

---

## ⚙️ Секция `run` - Общие настройки

```yaml
run:
  timeout: 5m           # Таймаут выполнения линтера
  tests: true           # Проверять тестовые файлы (*_test.go)
  build-tags:           # Build tags для компиляции
    - integration       # Включить код с тегом 'integration'
```

### Параметры:

| Параметр | Значение | Описание |
|----------|----------|----------|
| `timeout` | `5m` | Максимальное время выполнения. Увеличено для больших проектов |
| `tests` | `true` | Включает проверку тестовых файлов (`*_test.go`) |
| `build-tags` | `integration` | Позволяет проверять код с тегом `// +build integration` |

**Зачем это нужно:**
- **timeout**: Предотвращает зависание CI/CD pipeline
- **tests**: Важно проверять качество тестового кода
- **build-tags**: Интеграционные тесты должны соответствовать тем же стандартам качества

---

## 📝 Включенные линтеры

В проекте включено **14 линтеров**, каждый из которых проверяет определенные аспекты качества кода:

### 1. **govet** ⚠️ (Критический)
```yaml
- govet
```

**Что проверяет:**
- Официальный инструмент от команды Go
- Находит подозрительные конструкции кода
- Ошибки в форматировании `Printf/Sprintf`
- Неправильное использование `sync.Mutex`
- Недостижимый код (unreachable code)

**Пример проблемы:**
```go
// ❌ Плохо
fmt.Printf("User: %d", user.Name)  // %d для строки

// ✅ Хорошо
fmt.Printf("User: %s", user.Name)
```

---

### 2. **gofmt** 📐 (Форматирование)
```yaml
- gofmt
```

**Что проверяет:**
- Соответствие стандартному форматированию Go
- Отступы, пробелы, переносы строк

**Автофикс:**
```bash
gofmt -w .
# или
go fmt ./...
```

---

### 3. **goimports** 📦 (Импорты)
```yaml
- goimports
```

**Что проверяет:**
- Неиспользуемые импорты
- Отсутствующие импорты
- Правильная группировка импортов (stdlib, external, local)
- Сортировка импортов

**Пример:**
```go
// ❌ Плохо
import (
    "github.com/user/pr-reviewer/internal/models"
    "fmt"
    "github.com/gorilla/mux"
)

// ✅ Хорошо
import (
    "fmt"  // stdlib

    "github.com/gorilla/mux"  // external

    "github.com/user/pr-reviewer/internal/models"  // local
)
```

**Автофикс:**
```bash
goimports -w .
```

---

### 4. **misspell** 📝 (Опечатки)
```yaml
- misspell
```

**Что проверяет:**
- Опечатки в комментариях
- Опечатки в строках
- Опечатки в именах переменных

**Пример:**
```go
// ❌ Плохо
// Recieve data from database
func recieveData() {}

// ✅ Хорошо
// Receive data from database
func receiveData() {}
```

---

### 5. **ineffassign** 💡 (Неэффективные присваивания)
```yaml
- ineffassign
```

**Что проверяет:**
- Присваивания, которые никогда не используются
- Бесполезные переменные

**Пример:**
```go
// ❌ Плохо
func process() error {
    err := validateInput()
    err = saveToDatabase()  // Первое значение err потеряно
    return err
}

// ✅ Хорошо
func process() error {
    if err := validateInput(); err != nil {
        return err
    }
    return saveToDatabase()
}
```

---

### 6. **gosimple** 🎯 (Упрощения)
```yaml
- gosimple
```

**Что проверяет:**
- Код, который можно упростить
- Избыточные конструкции

**Примеры:**
```go
// ❌ Плохо
if x == true { }
for i, _ := range arr { }
if err != nil { return err } else { return nil }

// ✅ Хорошо
if x { }
for i := range arr { }
return err
```

---

### 7. **staticcheck** 🔬 (Статический анализ)
```yaml
- staticcheck
```

**Что проверяет:**
- Один из самых мощных линтеров
- Находит баги, проблемы производительности
- Устаревший код (deprecated APIs)
- Некорректное использование стандартной библиотеки

**Примеры:**
```go
// ❌ Плохо
resp, _ := http.Get(url)
// Body не закрывается - утечка ресурсов

// ✅ Хорошо
resp, err := http.Get(url)
if err != nil {
    return err
}
defer resp.Body.Close()
```

---

### 8. **unused** 🗑️ (Неиспользуемый код)
```yaml
- unused
```

**Что проверяет:**
- Неиспользуемые функции
- Неиспользуемые переменные
- Неиспользуемые константы
- Неиспользуемые типы

**Пример:**
```go
// ❌ Плохо
const unusedConst = 42  // Никогда не используется

func unusedFunc() {}    // Никогда не вызывается

// ✅ Хорошо - удалить неиспользуемый код
```

---

### 9. **gosec** 🔒 (Безопасность)
```yaml
- gosec
```

**Что проверяет:**
- Уязвимости безопасности
- Слабая криптография
- SQL инъекции
- Небезопасная работа с файлами
- Использование `math/rand` вместо `crypto/rand`

**Примеры проблем:**
```go
// ❌ Плохо - SQL инъекция
query := "SELECT * FROM users WHERE id = " + userId
db.Exec(query)

// ✅ Хорошо - параметризованный запрос
query := "SELECT * FROM users WHERE id = $1"
db.Exec(query, userId)
```

---

### 10. **bodyclose** 🚪 (Утечки ресурсов)
```yaml
- bodyclose
```

**Что проверяет:**
- Незакрытые `http.Response.Body`
- Утечки ресурсов в HTTP клиентах

**Пример:**
```go
// ❌ Плохо
resp, _ := http.Get(url)
data, _ := io.ReadAll(resp.Body)

// ✅ Хорошо
resp, err := http.Get(url)
if err != nil {
    return err
}
defer resp.Body.Close()
data, err := io.ReadAll(resp.Body)
```

---

### 11. **dupl** 📋 (Дублирование кода)
```yaml
- dupl
```

**Что проверяет:**
- Дублированный код
- Copy-paste фрагменты

**Настройка:**
- `threshold: 100` - минимум 100 токенов для обнаружения дубликата

**Пример:**
```go
// ❌ Плохо - дублирование логики
func GetUser(id int) (*User, error) {
    var user User
    err := db.QueryRow("SELECT * FROM users WHERE id = $1", id).Scan(&user)
    if err != nil {
        return nil, err
    }
    return &user, nil
}

func GetTeam(id int) (*Team, error) {
    var team Team
    err := db.QueryRow("SELECT * FROM teams WHERE id = $1", id).Scan(&team)
    if err != nil {
        return nil, err
    }
    return &team, nil
}

// ✅ Хорошо - обобщенная функция
func getEntity(query string, id int, dest interface{}) error {
    return db.QueryRow(query, id).Scan(dest)
}
```

---

### 12. **goconst** 🔢 (Магические константы)
```yaml
- goconst
```

**Что проверяет:**
- Повторяющиеся строки и числа
- "Магические" значения

**Пример:**
```go
// ❌ Плохо
if status == "OPEN" { }
if pr.Status == "OPEN" { }
if req.Status == "OPEN" { }

// ✅ Хорошо
const StatusOpen = "OPEN"

if status == StatusOpen { }
if pr.Status == StatusOpen { }
if req.Status == StatusOpen { }
```

---

### 13. **gocyclo** 🔄 (Цикломатическая сложность)
```yaml
- gocyclo
```

**Что проверяет:**
- Сложность функций
- Количество путей выполнения

**Настройка:**
- `min-complexity: 15` - максимум 15 независимых путей

**Почему это важно:**
- Функции с высокой цикломатической сложностью сложно тестировать
- Сложно понимать и поддерживать
- Высокий риск багов

**Пример проблемы:**
```go
// ❌ Плохо - сложность > 15
func ProcessRequest(req Request) error {
    if req.Type == "A" {
        if req.Status == "NEW" {
            if req.Priority == "HIGH" {
                // ... множество вложенных if
            }
        }
    } else if req.Type == "B" {
        // ... еще больше if
    }
    // ... и так далее
}

// ✅ Хорошо - разбить на меньшие функции
func ProcessRequest(req Request) error {
    switch req.Type {
    case "A":
        return processTypeA(req)
    case "B":
        return processTypeB(req)
    default:
        return ErrUnknownType
    }
}
```

---

### 14. **revive** 📏 (Стиль кода)
```yaml
- revive
```

**Что проверяет:**
- Соглашения по именованию
- Стиль комментариев
- Длина функций
- Количество параметров

**Примеры:**
```go
// ❌ Плохо
// get user from db
func GetUser(id int) (*User, error) {}

// ✅ Хорошо
// GetUser retrieves a user from the database by ID
func GetUser(id int) (*User, error) {}
```

---

### 15. **typecheck** ✅ (Проверка типов)
```yaml
- typecheck
```

**Что проверяет:**
- Ошибки типизации
- Несоответствие типов
- Отсутствующие импорты

---

## 🔧 Настройки линтеров (`linters-settings`)

### govet

```yaml
govet:
  check-shadowing: true
```

**check-shadowing** - обнаруживает затенение переменных:

```go
// ❌ Плохо
func process() error {
    err := validateInput()
    if needsValidation {
        err := checkPermissions()  // Затеняет внешний err
        if err != nil {
            return err
        }
    }
    return err  // Возвращает первоначальный err, а не ошибку checkPermissions
}

// ✅ Хорошо
func process() error {
    err := validateInput()
    if needsValidation {
        if err := checkPermissions(); err != nil {
            return err
        }
    }
    return err
}
```

---

### gofmt

```yaml
gofmt:
  simplify: true
```

**simplify** - упрощает код где возможно:

```go
// ❌ Плохо
var arr []int = []int{1, 2, 3}

// ✅ Хорошо (упрощено)
var arr = []int{1, 2, 3}
```

---

### goimports

```yaml
goimports:
  local-prefixes: github.com/user/pr-reviewer
```

**local-prefixes** - определяет локальные пакеты проекта.

Импорты будут группироваться:
1. Стандартная библиотека
2. Внешние зависимости
3. Локальные пакеты (с префиксом `github.com/user/pr-reviewer`)

```go
import (
    // 1. Стандартная библиотека
    "context"
    "fmt"
    "net/http"

    // 2. Внешние зависимости
    "github.com/gorilla/mux"
    "github.com/lib/pq"

    // 3. Локальные пакеты
    "github.com/user/pr-reviewer/internal/models"
    "github.com/user/pr-reviewer/internal/service"
)
```

---

### goconst

```yaml
goconst:
  min-len: 3              # Минимальная длина строки
  min-occurrences: 3      # Минимальное количество повторений
```

**Параметры:**
- **min-len: 3** - рассматривать строки от 3 символов
- **min-occurrences: 3** - если строка повторяется 3+ раз, сделать константой

**Пример:**
```go
// ❌ Плохо - строка "OPEN" повторяется 3+ раз
if pr.Status == "OPEN" { }
if status == "OPEN" { }
if req.Status == "OPEN" { }

// ✅ Хорошо
const StatusOpen = "OPEN"
```

---

### gocyclo

```yaml
gocyclo:
  min-complexity: 15
```

**min-complexity: 15** - максимальная цикломатическая сложность функции.

**Что означает сложность 15:**
- Функция имеет 15 независимых путей выполнения
- Для полного тестирования нужно минимум 15 тест-кейсов

**Как снизить сложность:**
1. Разбить большую функцию на несколько маленьких
2. Использовать `switch` вместо множества `if/else`
3. Извлечь логику в отдельные функции
4. Использовать таблицы и паттерны

---

### dupl

```yaml
dupl:
  threshold: 100
```

**threshold: 100** - минимум 100 токенов для обнаружения дубликата.

**Токен** - это элемент кода (переменная, оператор, ключевое слово).

---

### gosec

```yaml
gosec:
  excludes:
    - G104  # Ошибки неконтролируемые (unhandled errors)
    - G304  # Потенциально небезопасное открытие файлов
```

**Исключения:**

**G104** - игнорировать неконтролируемые ошибки:
```go
// Разрешено (иногда намеренно игнорируем)
_ = file.Close()
_ = rows.Close()
```

**G304** - игнорировать предупреждения о file path injection:
```go
// Разрешено (мы контролируем пути)
data, _ := os.ReadFile("migrations/001_schema.sql")
```

⚠️ **Внимание:** Исключайте эти правила только если уверены в безопасности!

---

## 🚫 Правила исключений (`issues`)

```yaml
issues:
  exclude-rules:
    - path: _test\.go          # Файлы тестов
      linters:
        - gosec               # Отключить проверки безопасности
        - dupl                # Отключить проверку дубликатов
    
    - path: internal/migrations/  # Папка миграций
      linters:
        - gosec               # Отключить проверки безопасности
```

### Зачем нужны исключения:

**1. Для тестов (`_test.go`):**
- **gosec** - в тестах часто используются небезопасные конструкции (хардкод паролей, тестовые данные)
- **dupl** - тестовый код часто похож (arrange-act-assert паттерн)

**Пример допустимого кода в тестах:**
```go
// ✅ Допустимо в _test.go
func TestLogin(t *testing.T) {
    password := "test123"  // Хардкод пароля в тесте - ОК
    token := "fake_jwt_token"  // ОК для тестов
}
```

**2. Для миграций (`internal/migrations/`):**
- **gosec** - миграции часто содержат SQL запросы, которые gosec может неправильно интерпретировать

---

## 🚀 Использование

### Локальный запуск

```bash
# Проверить весь проект
golangci-lint run

# Проверить конкретную директорию
golangci-lint run ./internal/...

# Проверить конкретный файл
golangci-lint run internal/service/service.go

# Запустить только определенные линтеры
golangci-lint run --enable=gosec,govet

# Исключить определенные линтеры
golangci-lint run --disable=dupl

# Показать все проблемы (без ограничения)
golangci-lint run --max-issues-per-linter=0 --max-same-issues=0

# Автофикс (где возможно)
golangci-lint run --fix
```

### Через Makefile

```bash
# Запустить линтер
make lint

# Форматировать код
make fmt

# Комбинация: форматирование + линтинг
make fmt lint
```

### В редакторе

#### VS Code

Установите расширение:
```
Go (golang.go)
```

Добавьте в `settings.json`:
```json
{
  "go.lintTool": "golangci-lint",
  "go.lintFlags": [
    "--fast"
  ],
  "go.lintOnSave": "package"
}
```

#### GoLand / IntelliJ IDEA

1. Settings → Go → Linter
2. Выбрать `golangci-lint`
3. Указать путь к конфигу: `.golangci.yml`

---

## 🔄 CI/CD интеграция

### GitHub Actions

```yaml
name: Lint

on: [push, pull_request]

jobs:
  golangci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: latest
          args: --timeout=5m
```

### GitLab CI

```yaml
lint:
  stage: test
  image: golangci/golangci-lint:latest
  script:
    - golangci-lint run --timeout=5m
  only:
    - merge_requests
    - main
```

### Pre-commit Hook

Создайте файл `.git/hooks/pre-commit`:

```bash
#!/bin/bash
golangci-lint run --new-from-rev=HEAD~1
```

```bash
chmod +x .git/hooks/pre-commit
```

---

## 📊 Статистика по линтерам

### Категории проверок

| Категория | Линтеры | Важность |
|-----------|---------|----------|
| **Корректность** | govet, typecheck, staticcheck | 🔴 Критическая |
| **Безопасность** | gosec | 🔴 Критическая |
| **Производительность** | bodyclose, ineffassign | 🟡 Высокая |
| **Качество кода** | gosimple, unused, dupl, gocyclo | 🟡 Высокая |
| **Стиль** | gofmt, goimports, revive, goconst | 🟢 Средняя |
| **Опечатки** | misspell | 🟢 Средняя |

---

## 💡 Рекомендации по работе

### 1. Запускайте линтер регулярно

```bash
# Перед каждым коммитом
make fmt lint

# Или автоматически через pre-commit hook
```

### 2. Исправляйте проблемы постепенно

```bash
# Сначала исправить критические
golangci-lint run --enable=govet,gosec

# Потом проблемы производительности
golangci-lint run --enable=bodyclose,ineffassign

# Потом стиль
golangci-lint run --enable=gofmt,goimports
```

### 3. Используйте автофикс где возможно

```bash
# Автоматически исправить проблемы форматирования
gofmt -w .
goimports -w .

# Или через golangci-lint
golangci-lint run --fix
```

### 4. Настройте CI/CD

Добавьте проверку линтера в CI pipeline, чтобы:
- ✅ Автоматически проверять каждый PR
- ✅ Блокировать merge при наличии критических проблем
- ✅ Поддерживать единый стиль кода в команде

### 5. Документируйте исключения

Если нужно отключить линтер для конкретной строки:

```go
//nolint:gosec // Причина исключения: тестовые данные
password := "test123"

//nolint:errcheck // Причина: intentional, error не критична
_ = file.Close()
```

### 6. Обновляйте линтер

```bash
# Обновить до последней версии
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

# Проверить версию
golangci-lint --version
```

---

## 🎓 Дополнительные ресурсы

- [Официальная документация golangci-lint](https://golangci-lint.run/)
- [Список всех доступных линтеров](https://golangci-lint.run/usage/linters/)
- [Effective Go](https://golang.org/doc/effective_go)
- [Go Code Review Comments](https://github.com/golang/go/wiki/CodeReviewComments)

---

## 📝 Итоги

Конфигурация линтера в проекте обеспечивает:

✅ **Качество кода** - обнаружение багов и anti-patterns  
✅ **Безопасность** - выявление уязвимостей  
✅ **Производительность** - обнаружение утечек ресурсов  
✅ **Поддерживаемость** - единый стиль кода  
✅ **Читаемость** - чистый и понятный код  

**Используйте линтер регулярно для поддержания высокого качества кода! 🚀**

